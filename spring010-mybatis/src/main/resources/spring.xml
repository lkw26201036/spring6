<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!--注册组件扫描器-->
    <context:component-scan base-package="com.lkw.bank"/>

    <!--外部属性配置文件-->
    <context:property-placeholder location="jdbc.properties"/>

    <!--数据源-->
    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!--SqlSessionFactoryBean-->
    <!--<bean class="org.mybatis.spring.SqlSessionFactoryBean">
        &lt;!&ndash;mybatis核心配置文件&ndash;&gt;
        <property name="configLocation" value="mybatis-config.xml"/>
        &lt;!&ndash;注入数据源&ndash;&gt;
        <property name="dataSource" ref="dataSource"/>
    </bean>

    &lt;!&ndash;mapper扫描器&ndash;&gt;
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.lkw.bank.mapper"/>
    </bean>-->

    <!--注入bean对象并赋值-->
    <bean id="account" class="com.lkw.bank.pojo.Account">
        <property name="actNo" value="1"/>
        <property name="balance" value="1000.0"/>
    </bean>
</beans>